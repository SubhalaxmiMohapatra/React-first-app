Counter project
Test case 1:
Increase number- for increate a number
create a component name as APP
inside the component use usestate hooks
set initial valuse as 0
and use a onClick action and use  setCount(count + 1)

Test case 2:
decrease number - for decrease a number
inside the app component create a button
and use a onClick action and use  setCount(count -1)

Test case 3:
reset number - for reset a number
inside the app component create a button
and use a onClick action and use   setCount(0)

component use: App